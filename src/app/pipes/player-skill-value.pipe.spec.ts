import { PlayerSkillValuePipe } from './player-skill-value.pipe';

describe('PlayerSkillValuePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerSkillValuePipe();
    expect(pipe).toBeTruthy();
  });
});
